import CustomSection from "@/components/common/custom-section";

export default function GateAccessLogs() {
  const settings = {
    title: "Gate Access Logs",
    description: "Monitor and track all gate access attempts",
  };

  return (
    <CustomSection {...settings}>
      <div></div>
    </CustomSection>
  );
}
