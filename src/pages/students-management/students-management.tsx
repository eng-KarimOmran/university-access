import CustomSection from "@/components/common/custom-section";

export default function StudentsManagement() {
  const settings = {
    title: "Students Management",
    description: "Manage student access and permissions",
  };

  return (
    <CustomSection {...settings}>
      <div></div>
    </CustomSection>
  );
}
